server {
    listen 8080;
    root /var/www/html;

    location /images {
        root /var/www/images;
        autoindex on;
        allow_methods GET;
    }

    location /upload {
        allow_methods POST;
        upload_store /var/www/uploads;
    }
}
